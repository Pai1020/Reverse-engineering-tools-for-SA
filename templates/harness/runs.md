# Analysis Harness Runs

> Index of all `start-analysis` runs, across every service in this workspace
> (or the single repo, if no `.workspace-profile.md` exists). Newest on top.
> One row per run. `service` is `—` in single-repo mode.

## Runs

| run_id | service | feature | entry_type | mode | started | status | re_analysis_count | last_stage | diff_rate | verify_round | quality_min | failed_quality | docs | ended |
|--------|---------|---------|------------|------|---------|--------|-------------------|------------|-----------|--------------|-------------|----------------|------|-------|
